# Разработка модуля семантического анализа текстов для ИИ-ассистента преподавателя

## Назначение проекта

Проект реализует модуль, который анализирует структуру и смысл учебных работ (курсовых) и выявляет потенциальные логические проблемы:

- слабая связность между соседними частями;
- несогласованность введения/заключения с основной частью;
- избыточное повторение смыслов.

Ключевая идея: модель эмбеддингов используется как инструмент представления смысла, а ядро диплома - это метод анализа (метрики + правила интерпретации).

## Текущий статус проекта

### Этап 0. Предобработка данных - выполнен

Реализовано:

- парсинг PDF и DOCX (`parser.py`);
- извлечение структуры документа (разделы/подразделы, номера, уровни);
- очистка текста (номера страниц, подписи/ссылки на рисунки, шум);
- исключение нерелевантных разделов (`Список литературы`, `Литература`, `Приложение`);
- разворачивание в плоские сегменты с ролями `intro/main/conclusion/other` (`preprocess_sections.py`);
- формирование итогового JSON с `segments` (`result.json`).

### Этап 1. Формирование эмбеддингов - выполнен

Реализовано:

- единый модуль расчёта эмбеддингов (`compute_embed.py`);
- поддержка двух режимов:
  - `SentenceTransformer`-модели;
  - обычные BERT-модели через mean-pooling;
- L2-нормализация векторов;
- сохранение:
  - `out/meta.json`;
  - `out/emb_<tag>.npy`.

Текущий набор моделей в коде:

- `ai-forever/FRIDA`;
- `ai-forever/sbert_large_nlu_ru`;
- `ai-forever/ruBERT-base`;
- `cointegrated/rubert-tiny2`;
- `sentence-transformers/paraphrase-multilingual-mpnet-base-v2`;
- `sentence-transformers/all-mpnet-base-v2`.

### Этап 2. Семантические метрики - выполнен (этап выбора рабочей модели)

В `versus_models.ipynb` реализован воспроизводимый протокол сравнения моделей:

- полная матрица косинусной близости `sim_<tag>.npy`;
- локальная связность соседних сегментов `adj_<tag>.npy`;
- оценки:
  - `intro -> main`,
  - `conclusion -> main`,
  - избыточность `main-main` (доля пар выше порога),
  - сравнение `adjacent` против случайных пар через `Cohen's d` и `KS`.
- устойчивость к случайности через multi-seed расчёт (агрегация `mean/std`);
- итоговый отчёт сравнения моделей `out/metrics_summary.json`.

Вывод этапа: по текущему корпусу данных и выбранному протоколу метрик модель `FRIDA` показала лучший результат и выбрана как рабочая для основной задачи.
Важно: это не «абсолютно лучшая модель вообще», а лучшая в рамках текущих данных, критериев и настроек эксперимента.

### Этап 3. Анализ документа и выявление логических проблем - в реализации (не завершён)

Этап 3 является основным смысловым этапом проекта.
На этом этапе выбранная модель эмбеддингов (`FRIDA`) используется как рабочий инструмент для решения целевой задачи:

- анализ структуры и смысловой связности документа;
- выявление потенциальных логических проблем;
- формирование интерпретируемого отчёта для преподавателя.

Что будет реализовано в рамках этапа:

- расчёт метрик проблем уже для конкретного документа, а не для сравнения моделей;
- обнаружение проблемных переходов между сегментами;
- проверка согласованности `intro/main/conclusion`;
- оценка семантической избыточности;
- выдача финального отчёта с локализацией проблемы и рекомендацией.

Текущий прогресс по этапу:

- создан рабочий ноутбук `compute_metrics.ipynb`;
- реализован базовый расчёт метрик для анализа одного документа на модели `FRIDA`;
- формируется отчёт `out/document_metrics_report.json` с флагами проблем и итоговым risk score.

Статус этапа: начат и функционален в базовом виде, но требует дальнейшей доработки (калибровка порогов, расширение правил интерпретации, стабилизация на расширенном корпусе).
Текущий фокус команды: активная работа ведётся на Этапе 3.
На данный момент к Этапу 3 относится только один рабочий файл: `compute_metrics.ipynb`.

### Фактические артефакты на текущих данных

- сегментов в `out/meta.json`: `11`;
- рассчитанные эмбеддинги:
  - `FRIDA`: `(11, 1536)`;
  - `sbert_large_nlu_ru`: `(11, 1024)`;
  - `rubert_base`: `(11, 768)`;
  - `rubert_tiny2`: `(11, 312)`;
  - `multi_mpnet_v2`: `(11, 768)`;
  - `all_mpnet_v2`: `(11, 768)`;
- сохранены промежуточные метрики:
  - `out/metrics_ready/sim_<tag>.npy`;
  - `out/metrics_ready/adj_<tag>.npy`.
- сохранён итоговый отчёт сравнения моделей:
  - `out/metrics_summary.json`.
- текущая выбранная рабочая модель для следующего этапа:
  - `FRIDA` (по `score_mean` в рамках текущего протокола).

## Что в старом плане уже устарело

- Неактуально: «сейчас только переходим к эмбеддингам». Эмбеддинги уже посчитаны для 6 моделей.
- Неактуально: «сначала выбрать много моделей». Базовый набор уже сформирован и протестирован на текущем корпусе.
- Неактуально: «этап 2 ещё в активной реализации». Этап 2 закрыт как этап отбора рабочей модели.
- Необязательно для этого диплома: сравнение с TF-IDF/RNN, если в теории уже обоснован фокус на Transformer/Sentence-BERT.

## Приоритетный план дальше (что, как, почему)

| Приоритет | Что сделать | Как сделать | Почему именно так |
|---|---|---|---|
| P0 | Реализовать Этап 3 в отдельном ноутбуке/модуле | Использовать `FRIDA` как рабочую модель и считать метрики уже для диагностики проблем документа | Этап 2 был промежуточным; основная ценность проекта в диагностике проблем |
| P0 | Перенести метрики из ноутбуков в модульный код | Создать `semantic_metrics.py` и `run_metrics.py`, убрать ручные ячейки | Повторяемость экспериментов и удобная защита на дипломе |
| P0 | Ввести правила интерпретации метрик | Описать пороги и условия вида `if ... then ...` для типов проблем | Это и есть объяснимая интеллектуальная часть модуля |
| P0 | Сформировать финальный формат отчёта | Отдельный JSON/текст: проблема, где найдена, значение метрики, рекомендация | Подготовка к интеграции в ИИ-ассистента преподавателя |
| P1 | Расширить корпус документов для эксперимента | Добавить несколько работ, прогонять пайплайн пакетно | На 1 документе невозможно сделать устойчивые выводы |
| P1 | Калибровка порогов на экспертной разметке | Сравнить результаты модуля с ручной оценкой преподавателя | Повышает научную обоснованность и качество системы |
| P1 | Лёгкое дообучение эмбеддингов (опционально) | Контрастивная настройка на парах `связано/не связано` (200-500 пар), затем сравнение до/после | Усиление модели без избыточной сложности обучения с нуля |
| P2 | LLM-слой для рекомендаций | LLM получает только уже найденные проблемы и метрики, а не «сырой текст» | Сохраняется интерпретируемость: решение принимает метод, не LLM |

## Как делать лёгкое дообучение (если решим включать)

Принцип:

- разметка `0/1` используется не как классификация, а как контрастивный сигнал для геометрии эмбеддингов;
- итоговые значения связности (диапазон 0..1) возникают после обучения через cosine similarity.

Минимально реалистичный сценарий:

1. Собрать пары полуавтоматически:
- `positive`: соседние логически связанные сегменты одной работы;
- `negative`: несвязанные фрагменты (в т.ч. из разных работ).

2. Проверить пары вручную (не размечать всё с нуля).

3. Дообучить базовую SBERT-модель и повторить те же метрики.

4. В дипломе показать сравнение `до/после` по одному протоколу.

## Идеи, которые не первоочередные, но полезные

- Визуализация карты связности документа (heatmap матрицы сходства).
- Автоматическая генерация «проблемных переходов» (top-N минимальных `adjacent`).
- Агрегированный скор документа с расшифровкой вклада каждой метрики.
- Небольшой веб-интерфейс для преподавателя (загрузка работы -> отчёт).

## Структура репозитория

- `parser.py` - парсинг PDF/DOCX и постобработка структуры.
- `preprocess_sections.py` - очистка и подготовка `segments`.
- `compute_embed.py` - расчёт и сохранение эмбеддингов.
- `make_embeddings.ipynb` - базовый сценарий генерации `result.json` и `out/*`.
- `versus_models.ipynb` - сравнение моделей и расчёт метрик.
- `result.json` - текущий пример подготовленных сегментов.
- `out/` - метаданные, эмбеддинги и промежуточные метрики.

## Форматы данных (текущие)

`result.json`:

- верхний уровень: `title`, `author`, `type`, `segments`;
- сегмент: `id`, `parent`, `path`, `role`, `text`.

`out/meta.json`:

- для каждого сегмента: `id`, `role`, `path`, `parent`.

`out/emb_<tag>.npy`:

- матрица эмбеддингов формы `(N, D)`, где:
  - `N` - число сегментов;
  - `D` - размерность модели.

## Зависимости

- `pdfplumber`
- `python-docx`
- `numpy`
- `torch`
- `transformers`
- `sentence-transformers`
- `jupyter`

## Текущие ограничения

1. Парсинг PDF зависит от качества распознавания страницы содержания.
2. Для DOCX ожидаются корректные стили `Heading 1/2` (`Заголовок 1/2`).
3. Оценка пока запускается в ноутбуках; нет отдельного воспроизводимого CLI-пайплайна метрик.
4. Нет `requirements.txt`/`pyproject.toml` с зафиксированными версиями.
5. Выбор `FRIDA` валиден для текущего корпуса и протокола; при расширении корпуса требуется повторная сверка моделей тем же методом.
